import { type CommandResult } from '../../cli/types';
export type AppInstallOptions = {
    appPath: string;
    remote?: string;
};
export declare const appInstall: (options: AppInstallOptions) => Promise<CommandResult>;
