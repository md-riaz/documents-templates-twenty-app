import { type CommandResult } from '../../cli/types';
export type ServerStartOptions = {
    port?: number;
    test?: boolean;
    version?: string;
    onProgress?: (message: string) => void;
};
export type ServerStartResult = {
    port: number;
    url: string;
};
export declare const serverStart: (options?: ServerStartOptions | undefined) => Promise<CommandResult<ServerStartResult>>;
