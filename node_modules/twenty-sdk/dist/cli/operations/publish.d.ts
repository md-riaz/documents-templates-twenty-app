import { type CommandResult } from '../../cli/types';
export type AppPublishOptions = {
    appPath: string;
    npmTag?: string;
    onProgress?: (message: string) => void;
};
export type AppPublishResult = {
    target: 'npm';
};
export declare const appPublish: (options: AppPublishOptions) => Promise<CommandResult<AppPublishResult>>;
