import { type CommandResult } from '../../cli/types';
export type AppBuildOptions = {
    appPath: string;
    tarball?: boolean;
    onProgress?: (message: string) => void;
};
export type AppBuildResult = {
    outputDir: string;
    fileCount: number;
    tarballPath?: string;
};
export declare const appBuild: (options: AppBuildOptions) => Promise<CommandResult<AppBuildResult>>;
