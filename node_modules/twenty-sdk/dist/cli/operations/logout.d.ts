import { type CommandResult } from '../../cli/types';
export type AuthLogoutOptions = {
    remote?: string;
};
export declare const authLogout: (options?: AuthLogoutOptions | undefined) => Promise<CommandResult>;
