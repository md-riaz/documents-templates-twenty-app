import { type CommandResult } from '../../cli/types';
export type AuthLoginOptions = {
    apiKey: string;
    apiUrl: string;
    remote?: string;
};
export declare const authLogin: (options: AuthLoginOptions) => Promise<CommandResult>;
