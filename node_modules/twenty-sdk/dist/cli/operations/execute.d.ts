import { type CommandResult, type FunctionExecutionResult } from '../../cli/types';
export type FunctionExecuteOptions = {
    appPath: string;
    remote?: string;
    payload?: Record<string, unknown>;
} & ({
    postInstall: true;
} | {
    preInstall: true;
} | {
    functionUniversalIdentifier: string;
} | {
    functionName: string;
});
export declare const functionExecute: (options: FunctionExecuteOptions) => Promise<CommandResult<FunctionExecutionResult>>;
