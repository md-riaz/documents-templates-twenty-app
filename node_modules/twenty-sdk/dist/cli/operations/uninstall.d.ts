import { type CommandResult } from '../../cli/types';
export type AppUninstallOptions = {
    appPath: string;
    remote?: string;
};
export declare const appUninstall: (options: AppUninstallOptions) => Promise<CommandResult>;
