type CallbackResult = {
    success: true;
    code: string;
} | {
    success: false;
    error: string;
};
type CallbackServer = {
    port: number;
    callbackUrl: string;
    waitForCallback: () => Promise<CallbackResult>;
    close: () => void;
};
export declare const startCallbackServer: (options?: {
    timeoutMs?: number | undefined;
} | undefined) => Promise<CallbackServer>;
export {};
