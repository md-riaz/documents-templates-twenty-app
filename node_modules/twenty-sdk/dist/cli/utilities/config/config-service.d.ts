export type RemoteConfig = {
    apiUrl: string;
    apiKey?: string;
    twentyCLIRegistrationId?: string;
    twentyCLIRegistrationClientId?: string;
    twentyCLIAccessToken?: string;
    twentyCLIRefreshToken?: string;
    appRegistrationId?: string;
    appRegistrationClientId?: string;
    appAccessToken?: string;
    appRefreshToken?: string;
};
export declare class ConfigService {
    private readonly configPath;
    private static activeRemote;
    constructor(options?: {
        configPath?: string;
    });
    static setActiveRemote(name?: string): void;
    static getActiveRemote(): string;
    private getActiveRemoteName;
    private readRawConfig;
    private migrateConfigIfNeeded;
    getConfig(): Promise<RemoteConfig>;
    getConfigForRemote(remoteName: string): Promise<RemoteConfig>;
    setConfig(config: Partial<RemoteConfig>): Promise<void>;
    clearConfig(): Promise<void>;
    private getDefaultConfig;
    getRemotes(): Promise<string[]>;
    getDefaultRemote(): Promise<string>;
    setDefaultRemote(name: string): Promise<void>;
}
