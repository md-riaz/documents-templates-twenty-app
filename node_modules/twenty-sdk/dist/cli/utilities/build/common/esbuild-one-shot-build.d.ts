import { type OnFileBuiltCallback } from '../../../../cli/utilities/build/common/restartable-watcher-interface';
import * as esbuild from 'esbuild';
import { type FileFolder } from 'twenty-shared/types';
export type EsbuildOneShotBuildOptions = {
    appPath: string;
    sourcePaths: string[];
    fileFolder: FileFolder;
    buildOptions: esbuild.BuildOptions;
    onFileBuilt: OnFileBuiltCallback;
};
export declare const esbuildOneShotBuild: ({ appPath, sourcePaths, fileFolder, buildOptions, onFileBuilt, }: EsbuildOneShotBuildOptions) => Promise<void>;
