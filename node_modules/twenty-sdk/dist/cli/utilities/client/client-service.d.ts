export declare class ClientService {
    private apiService;
    constructor(options?: {
        serverUrl?: string;
        token?: string;
        skipAuth?: boolean;
    });
    generateCoreClient({ appPath, appAccessToken }: {
        appPath: string;
        appAccessToken?: string;
    }): Promise<void>;
}
